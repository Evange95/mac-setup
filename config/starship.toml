# =============================================================================
# Starship Prompt Configuration
# =============================================================================

# Fail fast — don't block the prompt waiting for slow git
command_timeout = 500

add_newline = false

format = """
$directory\
$git_branch\
$git_status\
$python\
$nodejs\
$rust\
$golang\
$docker_context\
$kubernetes\
$aws\
$cmd_duration\
$line_break\
$character"""

# --- Prompt Character ---
[character]
success_symbol = "[>](bold green)"
error_symbol = "[>](bold red)"

# --- Directory ---
[directory]
truncation_length = 3
truncate_to_repo = true
style = "bold blue"

# --- Git ---
[git_branch]
format = "[$symbol$branch(:$remote_branch)]($style) "
style = "bold purple"

[git_status]
format = '([$all_status$ahead_behind]($style) )'
style = "bold red"
ignore_submodules = true

# --- Languages (detect by marker files only, not file extensions) ---
[nodejs]
format = "[$symbol($version)]($style) "
symbol = "node "
detect_extensions = []
detect_files = ["package.json"]
detect_folders = ["node_modules"]

[python]
format = "[$symbol($version)( \\($virtualenv\\))]($style) "
symbol = "py "
detect_extensions = []
detect_files = ["pyproject.toml", "requirements.txt", "setup.py", "setup.cfg", "Pipfile", ".python-version"]
detect_folders = [".venv", "venv"]

[rust]
format = "[$symbol($version)]($style) "
symbol = "rs "
detect_extensions = []
detect_files = ["Cargo.toml"]

[golang]
format = "[$symbol($version)]($style) "
symbol = "go "
detect_extensions = []
detect_files = ["go.mod"]

# --- Infrastructure ---
[docker_context]
format = "[$symbol$context]($style) "
symbol = "docker "
only_with_files = true

# Always visible when a kube context is set, so you know where kubectl will act.
# Production-looking contexts turn red.
[kubernetes]
disabled = false
format = "[$symbol$context( \\($namespace\\))]($style) "
symbol = "k8s "
style = "cyan"

[[kubernetes.contexts]]
context_pattern = ".*(prod|prd|live).*"
style = "bold red"

# Shown only when AWS_PROFILE / AWS_REGION / credentials env vars are set
[aws]
format = "[$symbol($profile)(\\($region\\))]($style) "
symbol = "aws "

# --- Command Duration ---
[cmd_duration]
min_time = 3000
format = "[$duration]($style) "
style = "bold yellow"

# --- Disabled modules ---
[package]
disabled = true

[gcloud]
disabled = true

[azure]
disabled = true
